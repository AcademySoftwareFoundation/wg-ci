versions:
  "1.2.1":
    folder: "all"
    variants:
      config:
        pkgref_namespace: null
      excluded_profiles:
        - vfx20
  "1.3":
    folder: "all"
    variants:
      config:
        pkgref_namespace: null
      excluded_profiles:
        - vfx20
  "1.4":
    folder: "all"
    variants:
      config:
        pkgref_namespace: null
      excluded_profiles:
        - vfx20
