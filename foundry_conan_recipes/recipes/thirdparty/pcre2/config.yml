versions:
  "10.42":
    folder: "all"
    variants:
      config:
        pkgref_namespace: null
      options:
        - shared: True
        - shared: False
      excluded_profiles:
        - vfx20
