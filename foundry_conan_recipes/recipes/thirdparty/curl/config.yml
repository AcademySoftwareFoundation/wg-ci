versions:
  "7.53.0":
    folder: "all"
    variants:
      options:
        - shared: True
          openssl_version: "1.1"
        - shared: False
          openssl_version: "1.1"
        - shared: True
          openssl_version: "1.0"
        - shared: False
          openssl_version: "1.0"
      only_profiles:
        - vfx20
  "7.72.0":
    folder: "all"
    variants:
      newer:
        options:
          - shared: True
            openssl_version: "1.1"
          - shared: False
            openssl_version: "1.1"
      vfx20:
        options:
          - shared: True
            openssl_version: "1.1"
          - shared: False
            openssl_version: "1.1"
          - shared: True
            openssl_version: "1.0"
          - shared: False
            openssl_version: "1.0"
        only_profiles:
          - vfx20
